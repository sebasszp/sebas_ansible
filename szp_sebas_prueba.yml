---
- name: Play 1
  hosts: localhost
  tasks:
    - set_fact:
        dato: "hola"

    - debug:
        var: dato

- name: Play 2
  hosts: all
  tasks:
    - debug:
        var: hostvars['localhost']['dato']
